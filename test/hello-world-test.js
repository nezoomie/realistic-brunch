var App = require('application');
    // chai = require('chai'),
    // assert = chai.assert;

describe('Hello World', function () {
    it('should run this test', function(done) {
      console.log(App);
      // assert(true);
      done();
    })
});